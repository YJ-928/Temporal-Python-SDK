Signal, Query, Update
Wait conditions, long running workflow
Child workflows, Continue_as_new
+ How does temporal handle multiple requests (async based) - as temporal workflows are blocking type as they wait for activity execution to complete.
+ How to create docker containers and execute them using temporal
+ How to run CLI based scripts in temporal workflow, in isolation 
+ zigflow

- Temporal Activity Loop
- 2 activites interdependent, output of activity one as input to activity two until desired result comes the loop repeats
- also added max retry policy for 10 times, so if desried result is coming in less than max attempts break the loop 
    or if max attempts reach break loop and then fail workflow.

- Signal Parallel processing in a single running workflow
- example:- Single workflow should be able to process multiple files parallely