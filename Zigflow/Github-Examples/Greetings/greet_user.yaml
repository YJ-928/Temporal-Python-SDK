document:
  dsl: 1.0.0
  taskQueue: zigflow-greeting
  workflowType: greet-user
  version: 0.0.1

do:
  - greet:
      set:
        message: ${"Hello " + $input.name }
      output:
        as:
          data: ${ . }
